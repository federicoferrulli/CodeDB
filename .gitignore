node_modules/
# Connessioni salvate: contiene password in chiaro, non versionare.
connections.ini
connections.ini.bak
connections.ini.bak2
# Audit log delle scritture eseguite via MCP.
mcp-audit.log
mcp-audit.log.1
# Audit log delle operazioni critiche/di scrittura eseguite via Web UI.
ui-audit.log
ui-audit.log.1
# Log del server avviato in background dal launcher desktop.
codedb.log
codedb.err.log
# Backup dei database creati da backup/cli.js (dati + log delle operazioni).
backups/
# Output di electron-builder (npm run dist:*): eseguibili/installer generati.
dist/
.agents
# Variabili d'ambiente per Docker Compose (puo' contenere GUI_MONGO_PASSPHRASE).
.env
